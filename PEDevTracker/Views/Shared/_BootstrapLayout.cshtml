<!DOCTYPE html>

<html lang="en">
<head>
    @Styles.Render("~/Content/bootstrap/base")
    @Styles.Render("~/Content/bootstrap/theme")
    @* Google Analytics code, can be placed anywhere, here is fine *@
    @{Html.RenderPartial("_GoogleAnalytics");}
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
</head>
<body>
    <div class="container">
        <div class="header">
        <ul class="nav nav-pills pull-right">
          <li>@Html.ActionLink("Home", "Index", "Home")</li>
          <li>@Html.ActionLink("Devs", "Index", "Developer")</li>
          <li>@Html.ActionLink("About", "About", "Home")</li>
        </ul>
        <h3 class="text-muted">Project: Eternity Dev Tracker</h3>
      </div>
        <div class="row">@RenderBody()</div>
    </div>
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
</body>
</html>
